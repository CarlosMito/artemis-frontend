import 'package:flutter/material.dart';

class DisplayImageOption extends StatelessWidget {
  const DisplayImageOption({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
