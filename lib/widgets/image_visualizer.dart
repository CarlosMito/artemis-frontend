import 'package:flutter/material.dart';

class ImageVisualizer extends StatelessWidget {
  const ImageVisualizer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
